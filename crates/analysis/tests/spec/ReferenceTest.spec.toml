# Expected behavior for ReferenceTest.ini: fields that name other
# definitions are Reference-typed, so the index drives completion and the
# unresolved-reference warning. Case-insensitivity of name resolution is
# pinned by the index unit tests.

# A bogus image name is flagged.
[[diag]]
severity = "warning"
code = "unresolved-reference"
on = "NoSuchImage"

# Rider fields are positional token lists: object and command-set tokens are references.
[[diag]]
severity = "warning"
code = "unresolved-reference"
on = "NoSuchRiderObject"

[[diag]]
severity = "warning"
code = "unresolved-reference"
on = "NoSuchCommandSet"

# `ButtonImage =` offers the workspace's mapped images.
[[complete]]
at = "$1"
includes = ["TestButtonImage"]

# `FireFX =` offers FX lists.
[[complete]]
at = "$2"
includes = ["FX_TestFire"]

# `FireSound =` offers audio events.
[[complete]]
at = "$3"
includes = ["TestFireSound"]

# A CommandSet slot offers command buttons.
[[complete]]
at = "$4"
includes = ["Command_RefTest"]

# Rider1 value positions complete by their typed token.
[[complete]]
at = "$5"
includes = ["RefTestObject"]

[[complete]]
at = "$6"
includes = ["RIDER1"]

[[complete]]
at = "$7"
includes = ["WEAPON_RIDER1"]

[[complete]]
at = "$8"
includes = ["STATUS_RIDER1"]

[[complete]]
at = "$9"
includes = ["RefTestCommandSet"]

[[complete]]
at = "$10"
includes = ["SET_NORMAL"]
