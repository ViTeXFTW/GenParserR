; Quickfix test: unresolved-reference stub creation.

; Resolved Upgrade (used to verify resolved refs don't get a stub).
Upgrade ExistingUpgrade
  BuildTime = 1.0
End

; Object with an unresolved Upgrade reference (triggers Create stub).
Object QFStubObject
  Behavior = WeaponSetUpgrade ModuleTag_01
    TriggeredBy = MissingUpgrade
    TriggeredBy = ExistingUpgrade
  End
End

; Weapon with an unresolved AudioEvent reference (ambiguous kind → no stub).
Weapon QFStubWeapon
  FireSound = MissingSound
End
