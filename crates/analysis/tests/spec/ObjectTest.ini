; `unkonwnUpgrade` is not an upgrade defined anywhere in this file.
Object TestObject1
    UpgradeCameo1 = unkonwnUpgrade
End

; Completion after `Draw =` should offer the available draw modules.
Object TestObject2
    Draw = $1
    End
End

; `Draw = W3DModelDraw` with no module tag should warn.
Object TestObject3
    Draw = W3DModelDraw
    End
End

; A WeaponSet with no Condition field should warn.
Object TestObject4
    WeaponSet
        Weapon = PRIMARY weaponName
    End
End

; Completion after `Weapon =` should offer the weapon slot enums.
Object TestObject5
    WeaponSet
        Conditions = None
        Weapon = $2
    End
End

; Cursor on a blank line inside a module body should offer module fields, not
; module type names — the blank line is body context, not header context.
Object TestObject6
    Draw = W3DModelDraw ModuleTag_01
        $3
    End
End
