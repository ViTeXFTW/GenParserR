# Expected behavior for ValueGoodTest.ini: every well-formed Phase-4 value
# (colors, coords, durations, percents, Armor coefficient pairs, WeaponSet /
# ArmorSet fields, Tracer nugget fields) passes without a single error.

no_errors = true
