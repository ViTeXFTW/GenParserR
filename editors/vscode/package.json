{
  "name": "zerosyntax-vscode",
  "displayName": "ZeroSyntax v2",
  "description": "Language support (diagnostics, completion, semantic highlighting) for C&C Generals: Zero Hour INI files, powered by ZeroSyntax v2.",
  "version": "1.0.2",
  "license": "MIT",
  "repository": {
    "type": "GitHub",
    "url": "https://github.com/ViTeXFTW/ZeroSyntaxV2"
  },
  "publisher": "ViTeXFTW",
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:generals-ini"
  ],
  "contributes": {
    "languages": [
      {
        "id": "generals-ini",
        "aliases": [
          "ZeroSyntax v2",
          "Generals INI",
          "generals-ini"
        ],
        "extensions": [
          ".ini"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "generals-ini",
        "scopeName": "source.generals-ini",
        "path": "./syntaxes/generals-ini.tmLanguage.json"
      }
    ],
    "configuration": {
      "title": "ZeroSyntax v2",
      "properties": {
        "zerosyntax.server.path": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute path to the `zerosyntax-lsp` server binary. If empty, the extension uses the bundled binary under `server/`, then falls back to `zerosyntax-lsp` on your PATH."
        },
        "zerosyntax.format.enable": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Enable document formatting (indentation normalization). When off — the default — the server does not advertise the formatting capability, so `#editor.formatOnSave#` will not invoke it for Generals INI files. Changing this restarts the language server."
        },
        "zerosyntax.baseIniRoots": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "markdownDescription": "Directories or `.big` archives containing base game/mod INI files that should be treated as already loaded before `map.ini`/`solo.ini`. Changing this restarts the language server."
        },
        "zerosyntax.trace.server": {
          "type": "string",
          "enum": [
            "off",
            "messages",
            "verbose"
          ],
          "default": "off",
          "description": "Trace the communication between VS Code and the language server."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "compile:test": "tsc -p ./tsconfig.test.json",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile",
    "package": "vsce package --allow-missing-repository",
    "test": "npm run compile && npm run compile:test && node ./out-test/src/test/runTest.js"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.84.0",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "mocha": "^11.7.6",
    "typescript": "^5.4.0"
  }
}